# Seção de Eventos — plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publicar uma aba "Eventos" na página de locação, com uma listagem `/eventos` e 16 páginas de case, cada uma com SEO próprio, geradas a partir de um `eventos.json` revisável.

**Architecture:** Dados em JSON → gerador Node → HTML estático, exatamente o padrão que `tools/gerar-loja.js` já usa em produção. Um módulo de dados compartilhado (`tools/eventos-dados.js`) é importado tanto pelo gerador de eventos quanto pelo gerador da loja, que é quem escreve o `sitemap.xml` — assim página nova e URL no sitemap nunca saem de sincronia. A mídia é convertida fora do build por um script sob demanda, porque as pastas de origem não existem na máquina de build da Vercel.

**Tech Stack:** Node.js (sem dependências — só `node:fs`, `node:path`, `node:child_process`), `node --test` para testes, `ffmpeg` e `sips` para mídia, HTML/CSS estático, deploy na Vercel.

**Spec:** `docs/superpowers/specs/2026-09-11-eventos-design.md`

## Global Constraints

- **Sem dependências novas.** O `package.json` não tem `dependencies` e continua assim. Só módulos nativos do Node.
- **Slug ASCII obrigatório.** Nome de arquivo ou rota com acento funciona no Mac e dá 404 na Vercel. Todo slug e todo nome de arquivo de mídia: `[a-z0-9-]+` apenas.
- **Caminhos absolutos nas páginas geradas.** O `index.html` usa caminhos relativos (`assets/js/app.js`). Uma página em `/eventos/<slug>/` está dois níveis abaixo da raiz — todo `src`/`href` de asset nas páginas geradas tem que começar com `/`.
- **Telefone / WhatsApp:** `5544988117615`. Já é o número em uso no `index.html` e no `gerar-loja.js`.
- **Domínio:** `https://nuvemair.com.br`, via `process.env.SITE_URL` com esse default — mesma convenção do `gerar-loja.js`.
- **Idioma:** todo identificador, comentário e mensagem de commit em português, como o resto do repositório.
- **`publicar: false` até liberação.** Enquanto estiver `false`, listagem e cases saem com `noindex, follow` e ficam fora do sitemap.
- **Nada de `Event` no JSON-LD.** Cases usam `Article` + `BreadcrumbList`. `Event` descreve evento futuro e geraria rich result errado.
- **Não tocar** em `/comprar`, `/loja`, `api/`, nem em seções do `index.html` além do nav.

## Estrutura de arquivos

| Arquivo | Responsabilidade |
|---|---|
| `tools/midia-eventos.json` | Curadoria: mapa slug → pasta de origem + arquivos escolhidos. Entrada do script de mídia. |
| `tools/preparar-midia-eventos.js` | Converte a mídia escolhida para `assets/eventos/<slug>/`. Roda sob demanda, fora do build. |
| `assets/eventos/<slug>/*.mp4 .webp` | Mídia convertida, commitada. |
| `eventos.json` | Dados dos 16 cases. **O arquivo que o usuário revisa.** |
| `tools/eventos-dados.js` | Carrega, valida e deriva (URLs de sitemap, cases irmãos). Importado pelo gerador de eventos **e** pelo `gerar-loja.js`. |
| `tools/site-partes.js` | Extrai nav e rodapé do `index.html` e absolutiza caminhos. Fonte única de cabeçalho/rodapé. |
| `tools/gerar-eventos.js` | Escreve `eventos/index.html` e `eventos/<slug>/index.html`. |
| `assets/css/eventos.css` | CSS exclusivo da seção. |
| `tests/eventos.test.js` | Testes do módulo de dados. |
| `tests/site-partes.test.js` | Testes da extração de partes. |

Ordem das tasks: mídia primeiro (Task 1), porque a validação dos dados (Task 2) confere se cada arquivo referenciado existe em disco.

---

### Task 1: Pipeline de mídia

Converte a mídia crua das pastas fora do repositório para formato web. As fontes são 4K/60fps HEVC com áudio (iPhone) e HEIC de 4032 px; o site usa H.264 ~1000 kb/s sem áudio e WebP.

**Files:**
- Create: `tools/midia-eventos.json`
- Create: `tools/preparar-midia-eventos.js`
- Create: `assets/eventos/<slug>/` (saída, 16 pastas)
- Create: `assets/eventos/manifest.json` (saída, consumida pela Task 2)

**Interfaces:**
- Consumes: nada de tasks anteriores.
- Produces: `assets/eventos/manifest.json`, no formato
  `{ "<slug>": [{ "tipo": "video"|"imagem", "arquivo": "<nome>.mp4"|"<nome>.webp", "poster": "<nome>.webp"|null }] }`.
  A Task 2 lê esse manifesto para preencher o campo `midia` do `eventos.json`.

- [ ] **Step 1: Confirmar que as ferramentas existem**

```bash
which ffmpeg && ffmpeg -hide_banner -encoders | grep -E "libx264|libwebp" && which sips
```

Esperado: caminho do `ffmpeg`, as linhas `libx264` e `libwebp`, e `/usr/bin/sips`. Se `libwebp` não aparecer, pare e avise — o preset de poster depende dele.

- [ ] **Step 2: Gerar as miniaturas de curadoria**

Antes de escolher, é preciso ver. Extrai um frame do segundo 1 de cada vídeo e uma miniatura de cada foto, num diretório de rascunho fora do repositório:

```bash
RASCUNHO="${TMPDIR:-/tmp}/curadoria-eventos"
ORIGEM="/Users/gustavo/Desktop/Empresas/Nuvem Air/Marketing"
mkdir -p "$RASCUNHO"
cd "$ORIGEM"
find Eventos Mensal -type f \( -iname "*.MOV" -o -iname "*.MP4" \) -print0 | while IFS= read -r -d '' f; do
  nome=$(echo "$f" | tr '/ ' '__' | tr -d "'")
  ffmpeg -v error -y -ss 1 -i "$f" -frames:v 1 -vf "scale=320:-2" "$RASCUNHO/${nome}.jpg"
done
find Eventos Mensal -type f -iname "*.HEIC" -print0 | while IFS= read -r -d '' f; do
  nome=$(echo "$f" | tr '/ ' '__' | tr -d "'")
  sips -s format jpeg -Z 320 "$f" --out "$RASCUNHO/${nome}.jpg" >/dev/null
done
ls "$RASCUNHO" | wc -l
```

Esperado: ~95 arquivos `.jpg`.

- [ ] **Step 3: Escolher os arquivos e escrever `tools/midia-eventos.json`**

Abra as miniaturas do passo anterior e escolha, por case, **até 4 arquivos** — priorizando: (a) equipamento Nuvem Air visível e em operação, (b) ambiente reconhecível ao fundo, (c) foto antes de vídeo quando as duas mostram a mesma coisa (foto pesa menos).

Dois cases têm um único arquivo de origem (`colegio-adventista`, `ecospeed`); nesses, o mínimo é 1. A spec fala em "3–5 por case" — esses dois são a exceção, e é esperado.

Formato exato (as chaves são os 16 slugs da spec; `origem` é relativa a `Marketing/`):

```json
{
  "casamento-em-loanda": {
    "origem": "Eventos/Casamento Loanda",
    "arquivos": ["IMG_1473 2.HEIC", "IMG_1474 2.MOV"]
  },
  "colegio-adventista": {
    "origem": "Eventos/Colegio Adventista",
    "arquivos": ["IMG_6744.MOV"]
  }
}
```

As 16 pastas de origem, na ordem da spec:

| slug | origem |
|---|---|
| `motor-classic` | `Eventos/Motor Classic` |
| `companhia-de-melhoramentos` | `Eventos/Evento Compahia de Melhoramentos ` |
| `rotary-sicredi` | `Eventos/Rotary Sicreed` |
| `rotary-acema` | `Eventos/Evento Rotary Acema` |
| `hospital-universitario` | `Eventos/Hospital Universitario` |
| `weld-vision` | `Eventos/Evento Weld Vision 03` |
| `agro-semente` | `Eventos/Evento Agro Semente` |
| `havam` | `Eventos/Havam` |
| `ecospeed` | `Eventos/Ecospeed` |
| `ciacollor` | `Eventos/Ciacollor` |
| `colegio-adventista` | `Eventos/Colegio Adventista` |
| `quinta-dos-mouroes` | `Eventos/Quinta dos Mouroes` |
| `chacara-jandaia` | `Eventos/Evento Chacara Jandaia` |
| `confraternizacao-empresa-jandaia` | `Eventos/Confraterzinaçao Empresa Jandaia` |
| `casamento-em-loanda` | `Eventos/Casamento Loanda` |
| `soft-dream-tecelagem` | `Mensal/Soft Dream Tecelagem` |

Atenção: `Eventos/Evento Compahia de Melhoramentos ` termina com **espaço**. É o nome real da pasta.

- [ ] **Step 4: Escrever `tools/preparar-midia-eventos.js`**

```js
#!/usr/bin/env node
/* Converte a mídia crua dos eventos para formato web.  `npm run midia`
 *
 * Por que fica fora do `npm run build`: as pastas de origem vivem no Desktop,
 * fora do repositório, e não existem na máquina de build da Vercel. O que a
 * Vercel vê é o resultado já convertido e commitado em assets/eventos/.
 *
 * Preset copiado do que já está em produção na home: H.264 ~1000 kb/s, sem
 * faixa de áudio, faststart. As fontes são 4K/60fps HEVC com áudio estéreo;
 * sem conversão, um único arquivo pesaria mais que a página inteira.
 */
'use strict';

const fs = require('node:fs');
const path = require('node:path');
const { execFileSync } = require('node:child_process');

const RAIZ = path.join(__dirname, '..');
/* As pastas de origem ficam dois níveis acima do site, em Marketing/. */
const ORIGEM = process.env.MIDIA_ORIGEM || path.join(RAIZ, '..');
const DESTINO = path.join(RAIZ, 'assets', 'eventos');

const curadoria = JSON.parse(fs.readFileSync(path.join(__dirname, 'midia-eventos.json'), 'utf8'));

const ff = (args) => execFileSync('ffmpeg', ['-v', 'error', '-y'].concat(args), { stdio: 'inherit' });

/* Caixa de 1280x1280 com aspecto preservado: retrato vira 720x1280, paisagem
   vira 1280x720. As fontes misturam as duas orientações, então fixar largura
   OU altura distorceria metade do acervo. force_divisible_by=2 porque H.264
   com yuv420p exige dimensão par. */
const ESCALA_VIDEO = 'scale=w=1280:h=1280:force_original_aspect_ratio=decrease:force_divisible_by=2';

function converterVideo(entrada, saida) {
  ff(['-i', entrada,
      '-t', '8',              // até 8s: é loop mudo de vitrine, não filme
      '-an',                  // sem áudio, como os vídeos que já estão na home
      '-vf', ESCALA_VIDEO,
      '-r', '30',
      '-c:v', 'libx264', '-profile:v', 'high', '-pix_fmt', 'yuv420p',
      '-b:v', '1000k', '-maxrate', '1400k', '-bufsize', '2000k',
      '-movflags', '+faststart',
      saida]);
}

function gerarPoster(video, saida) {
  ff(['-ss', '1', '-i', video, '-frames:v', '1',
      '-vf', 'scale=w=420:h=420:force_original_aspect_ratio=decrease:force_divisible_by=2',
      '-c:v', 'libwebp', '-quality', '80', saida]);
}

/* HEIC o ffmpeg não lê; o sips (nativo do macOS) decodifica para JPEG e o
   ffmpeg faz o resto. */
function converterFoto(entrada, saida) {
  const temp = path.join(DESTINO, '.temp.jpg');
  execFileSync('sips', ['-s', 'format', 'jpeg', '-s', 'formatOptions', '92', entrada, '--out', temp], { stdio: 'ignore' });
  ff(['-i', temp,
      '-vf', 'scale=w=1200:h=1200:force_original_aspect_ratio=decrease:force_divisible_by=2',
      '-c:v', 'libwebp', '-quality', '80', saida]);
  fs.unlinkSync(temp);
}

const manifesto = {};

for (const slug of Object.keys(curadoria)) {
  const { origem, arquivos } = curadoria[slug];
  const pastaSaida = path.join(DESTINO, slug);
  fs.mkdirSync(pastaSaida, { recursive: true });
  manifesto[slug] = [];

  arquivos.forEach((nome, i) => {
    const entrada = path.join(ORIGEM, origem, nome);
    if (!fs.existsSync(entrada)) throw new Error(`origem não encontrada: ${entrada}`);

    const base = `${slug}-${i + 1}`;
    const ext = path.extname(nome).toLowerCase();

    if (ext === '.mov' || ext === '.mp4') {
      const mp4 = path.join(pastaSaida, `${base}.mp4`);
      const poster = path.join(pastaSaida, `${base}.webp`);
      converterVideo(entrada, mp4);
      gerarPoster(mp4, poster);
      manifesto[slug].push({ tipo: 'video', arquivo: `${base}.mp4`, poster: `${base}.webp` });
      console.log('  -> %s (%d KB)', `${slug}/${base}.mp4`, Math.round(fs.statSync(mp4).size / 1024));
    } else {
      const webp = path.join(pastaSaida, `${base}.webp`);
      converterFoto(entrada, webp);
      manifesto[slug].push({ tipo: 'imagem', arquivo: `${base}.webp`, poster: null });
      console.log('  -> %s (%d KB)', `${slug}/${base}.webp`, Math.round(fs.statSync(webp).size / 1024));
    }
  });
}

fs.writeFileSync(path.join(DESTINO, 'manifest.json'), JSON.stringify(manifesto, null, 2) + '\n');
console.log('\n%d cases, %d arquivos -> assets/eventos/',
  Object.keys(manifesto).length,
  Object.values(manifesto).reduce((n, m) => n + m.length, 0));
```

- [ ] **Step 5: Registrar o script no `package.json`**

Em `package.json`, dentro de `scripts`, adicione a linha `midia` (o `build` fica como está — mídia não entra no build):

```json
  "scripts": {
    "build": "node tools/gerar-loja.js",
    "midia": "node tools/preparar-midia-eventos.js",
    "test": "node --test tests/*.test.js"
  }
```

- [ ] **Step 6: Rodar a conversão**

```bash
npm run midia
```

Esperado: uma linha por arquivo com o tamanho em KB, e o resumo final. Os `.mp4` devem sair na faixa de 300 KB–1,2 MB. Se algum passar de 2 MB, o vídeo de origem tem movimento demais para 1000 kb/s — reduza `-t` para 5 nesse arquivo ou escolha outro na curadoria.

- [ ] **Step 7: Conferir o peso total**

```bash
du -sh assets/eventos && find assets/eventos -type f | wc -l
```

Esperado: entre 25 MB e 70 MB. Acima de 100 MB, revise a curadoria para menos arquivos por case — a spec orçou 55–70 MB.

- [ ] **Step 8: Commit**

```bash
git add tools/midia-eventos.json tools/preparar-midia-eventos.js package.json assets/eventos
git commit -m "Converte a mídia dos eventos para formato web

Curadoria de até 4 arquivos por case, das pastas Eventos/ e Mensal/ que
vivem fora do repositório. Preset igual ao dos vídeos que já estão na
home: H.264 ~1000 kb/s, sem áudio, mais poster WebP. O script fica fora
do npm run build porque a origem não existe na máquina de build."
```

---

### Task 2: Dados dos cases e validação

**Files:**
- Create: `eventos.json`
- Create: `tools/eventos-dados.js`
- Create: `tests/eventos.test.js`

**Interfaces:**
- Consumes: `assets/eventos/manifest.json` (Task 1).
- Produces — a API que as Tasks 3 a 8 usam:
  - `carregar(opcoes = {})` → `{ publicar: boolean, cases: Case[] }`. `opcoes.raiz` sobrescreve a raiz do projeto (para teste).
  - `validar(dados, opcoes = {})` → lança `Error` na primeira violação; devolve `dados` se tudo passar. `opcoes.raiz` habilita a checagem de mídia em disco.
  - `urlsSitemap(dados, site)` → `[{ loc: string, prioridade: string }]`. Lista vazia se `publicar` for `false`.
  - `irmaos(dados, slug, n = 2)` → `Case[]` com até `n` cases, mesma `categoria` primeiro, nunca incluindo o próprio.
  - `CATEGORIAS` → `['casamento','corporativo','industria','galpao','institucional']`
  - `TIPOS` → `['evento','mensal']`
  - Um `Case` tem: `slug, tipo, categoria, cliente, cidade, uf, data, equipamento[], quantidade, area, desafio, solucao, resultado, midia[], seo{titulo,descricao,h1}`.

- [ ] **Step 1: Escrever os testes que falham**

Crie `tests/eventos.test.js`:

```js
const test = require('node:test');
const assert = require('node:assert');
const path = require('node:path');
const dados = require('../tools/eventos-dados.js');

function caseValido(extra) {
  return Object.assign({
    slug: 'casamento-em-loanda',
    tipo: 'evento',
    categoria: 'casamento',
    cliente: 'Casamento em Loanda',
    cidade: 'Loanda',
    uf: 'PR',
    data: '2025-11',
    equipamento: ['Climatizador Industrial Compacto'],
    quantidade: '4',
    area: '300 m²',
    desafio: 'Recepção ao ar livre em novembro.',
    solucao: 'Quatro climatizadores nas laterais da tenda.',
    resultado: 'Ambiente a 6 °C abaixo da temperatura externa.',
    midia: [{ tipo: 'imagem', arquivo: 'casamento-em-loanda-1.webp', poster: null, alt: 'Climatizador em casamento' }],
    seo: {
      titulo: 'Aluguel de climatizador para casamento em Loanda · Nuvem Air',
      descricao: 'Como a Nuvem Air climatizou um casamento em Loanda, no Paraná.',
      h1: 'Um casamento em Loanda sem ninguém abanando o leque'
    }
  }, extra);
}

const base = (cases, publicar = true) => ({ publicar, cases });

test('aceita um conjunto válido', () => {
  assert.doesNotThrow(() => dados.validar(base([caseValido()])));
});

test('recusa slug com acento', () => {
  assert.throws(() => dados.validar(base([caseValido({ slug: 'casamento-loandá' })])), /slug/i);
});

test('recusa slug com maiúscula ou espaço', () => {
  assert.throws(() => dados.validar(base([caseValido({ slug: 'Casamento Loanda' })])), /slug/i);
});

test('recusa slug duplicado', () => {
  assert.throws(() => dados.validar(base([caseValido(), caseValido()])), /duplicad/i);
});

test('recusa categoria fora do vocabulário', () => {
  assert.throws(() => dados.validar(base([caseValido({ categoria: 'festa' })])), /categoria/i);
});

test('recusa tipo fora do vocabulário', () => {
  assert.throws(() => dados.validar(base([caseValido({ tipo: 'anual' })])), /tipo/i);
});

test('recusa case sem mídia', () => {
  assert.throws(() => dados.validar(base([caseValido({ midia: [] })])), /mídia/i);
});

test('recusa título de SEO vazio', () => {
  const c = caseValido();
  c.seo = Object.assign({}, c.seo, { titulo: '' });
  assert.throws(() => dados.validar(base([c])), /seo\.titulo/i);
});

test('recusa títulos de SEO repetidos entre cases', () => {
  const a = caseValido();
  const b = caseValido({ slug: 'outro-case' });
  assert.throws(() => dados.validar(base([a, b])), /t[ií]tulo/i);
});

test('recusa REVISAR pendente quando publicar é true', () => {
  assert.throws(() => dados.validar(base([caseValido({ cidade: 'REVISAR: Loanda' })])), /REVISAR/);
});

test('tolera REVISAR pendente quando publicar é false', () => {
  assert.doesNotThrow(() => dados.validar(base([caseValido({ cidade: 'REVISAR: Loanda' })], false)));
});

test('urlsSitemap devolve a listagem mais um por case quando publicar é true', () => {
  const u = dados.urlsSitemap(base([caseValido(), caseValido({ slug: 'outro', seo: Object.assign({}, caseValido().seo, { titulo: 'Outro título' }) })]), 'https://nuvemair.com.br');
  assert.strictEqual(u.length, 3);
  assert.strictEqual(u[0].loc, 'https://nuvemair.com.br/eventos');
  assert.strictEqual(u[0].prioridade, '0.8');
  assert.strictEqual(u[1].loc, 'https://nuvemair.com.br/eventos/casamento-em-loanda');
  assert.strictEqual(u[1].prioridade, '0.7');
});

test('urlsSitemap devolve vazio quando publicar é false', () => {
  assert.deepStrictEqual(dados.urlsSitemap(base([caseValido()], false), 'https://nuvemair.com.br'), []);
});

test('irmaos prefere a mesma categoria e nunca devolve o próprio', () => {
  const conj = base([
    caseValido({ slug: 'a' }),
    caseValido({ slug: 'b', categoria: 'industria', seo: { titulo: 'T b', descricao: 'd', h1: 'h' } }),
    caseValido({ slug: 'c', seo: { titulo: 'T c', descricao: 'd', h1: 'h' } }),
    caseValido({ slug: 'd', seo: { titulo: 'T d', descricao: 'd', h1: 'h' } })
  ]);
  const r = dados.irmaos(conj, 'a', 2);
  assert.strictEqual(r.length, 2);
  assert.ok(!r.some((x) => x.slug === 'a'));
  assert.deepStrictEqual(r.map((x) => x.categoria), ['casamento', 'casamento']);
});

test('o eventos.json real do projeto passa na validação, mídia em disco incluída', () => {
  const reais = dados.carregar();
  assert.ok(reais.cases.length === 16, 'esperado 16 cases, veio ' + reais.cases.length);
  assert.doesNotThrow(() => dados.validar(reais, { raiz: path.join(__dirname, '..') }));
});
```

- [ ] **Step 2: Rodar os testes e ver falhar**

```bash
npm test
```

Esperado: FAIL — `Cannot find module '../tools/eventos-dados.js'`.

- [ ] **Step 3: Escrever `tools/eventos-dados.js`**

```js
/* Dados dos cases de evento — fonte única.
 *
 * Por que é um módulo e não leitura direta do JSON: quem escreve o
 * sitemap.xml é o tools/gerar-loja.js, não o gerador de eventos. Se cada um
 * mantivesse sua lista de URLs, página nova entraria no site sem entrar no
 * sitemap. Os dois importam daqui. É a mesma razão pela qual o gerar-loja.js
 * importa loja-core.js em vez de recalcular preço.
 */
'use strict';

const fs = require('node:fs');
const path = require('node:path');

const CATEGORIAS = ['casamento', 'corporativo', 'industria', 'galpao', 'institucional'];
const TIPOS = ['evento', 'mensal'];
const SLUG_VALIDO = /^[a-z0-9]+(-[a-z0-9]+)*$/;

/* Campos de texto que vão para a página e, por isso, não podem sair com
   marcação de revisão pendente. */
const CAMPOS_TEXTO = ['cliente', 'cidade', 'uf', 'data', 'quantidade', 'area', 'desafio', 'solucao', 'resultado'];

function carregar(opcoes = {}) {
  const raiz = opcoes.raiz || path.join(__dirname, '..');
  return JSON.parse(fs.readFileSync(path.join(raiz, 'eventos.json'), 'utf8'));
}

function validar(dados, opcoes = {}) {
  if (!dados || !Array.isArray(dados.cases)) throw new Error('eventos.json: "cases" precisa ser uma lista');

  const vistos = new Set();
  const titulos = new Set();

  for (const c of dados.cases) {
    if (!SLUG_VALIDO.test(String(c.slug || ''))) {
      throw new Error(`slug inválido: ${JSON.stringify(c.slug)} — use só a-z, 0-9 e hífen (acento vira 404 na Vercel)`);
    }
    if (vistos.has(c.slug)) throw new Error(`slug duplicado: ${c.slug}`);
    vistos.add(c.slug);

    if (!TIPOS.includes(c.tipo)) throw new Error(`${c.slug}: tipo inválido "${c.tipo}" — use ${TIPOS.join(' ou ')}`);
    if (!CATEGORIAS.includes(c.categoria)) {
      throw new Error(`${c.slug}: categoria inválida "${c.categoria}" — use uma de ${CATEGORIAS.join(', ')}`);
    }

    if (!Array.isArray(c.midia) || c.midia.length === 0) throw new Error(`${c.slug}: sem mídia`);

    const seo = c.seo || {};
    for (const campo of ['titulo', 'descricao', 'h1']) {
      if (!String(seo[campo] || '').trim()) throw new Error(`${c.slug}: seo.${campo} vazio`);
    }
    if (titulos.has(seo.titulo)) throw new Error(`título de SEO repetido: "${seo.titulo}" — cada case precisa do seu`);
    titulos.add(seo.titulo);

    /* Com publicar:false as páginas saem noindex e fora do sitemap, então
       rascunho ainda é aceitável. Ligar publicar é o gesto que exige o
       conteúdo revisado. */
    if (dados.publicar) {
      for (const campo of CAMPOS_TEXTO.concat(['seo'])) {
        const valor = campo === 'seo' ? Object.values(seo).join(' ') : c[campo];
        if (String(valor == null ? '' : valor).includes('REVISAR')) {
          throw new Error(`${c.slug}: campo "${campo}" ainda tem REVISAR — revise antes de publicar`);
        }
      }
    }

    if (opcoes.raiz) {
      for (const m of c.midia) {
        for (const nome of [m.arquivo, m.poster].filter(Boolean)) {
          const caminho = path.join(opcoes.raiz, 'assets', 'eventos', c.slug, nome);
          if (!fs.existsSync(caminho)) throw new Error(`${c.slug}: mídia ausente em disco — ${caminho}`);
        }
      }
    }
  }

  return dados;
}

function urlsSitemap(dados, site) {
  if (!dados.publicar) return [];
  return [{ loc: site + '/eventos', prioridade: '0.8' }]
    .concat(dados.cases.map((c) => ({ loc: `${site}/eventos/${c.slug}`, prioridade: '0.7' })));
}

function irmaos(dados, slug, n = 2) {
  const eu = dados.cases.find((c) => c.slug === slug);
  const outros = dados.cases.filter((c) => c.slug !== slug);
  const mesma = outros.filter((c) => eu && c.categoria === eu.categoria);
  const resto = outros.filter((c) => !eu || c.categoria !== eu.categoria);
  return mesma.concat(resto).slice(0, n);
}

module.exports = { carregar, validar, urlsSitemap, irmaos, CATEGORIAS, TIPOS };
```

- [ ] **Step 4: Rodar os testes — devem passar menos o último**

```bash
npm test
```

Esperado: tudo PASS exceto `o eventos.json real do projeto passa na validação`, que falha com `ENOENT ... eventos.json`. É o esperado: o arquivo de dados é o próximo passo.

- [ ] **Step 5: Escrever o `eventos.json` com os 16 cases**

Comece do manifesto da Task 1, que já traz `midia` pronto:

```bash
cat assets/eventos/manifest.json
```

Regras de preenchimento:

- **`publicar`: `false`** no topo. Só o usuário liga isso.
- **Verificável na mídia** (ambiente, equipamento visível, quantidade contável na imagem): escreva o valor direto.
- **Não verificável** (cidade, data, m², número total de equipamentos): escreva `REVISAR: <melhor palpite>`. O palpite ajuda o usuário a corrigir mais rápido do que um campo em branco.
- **`alt` de cada mídia**: descreva o que está na imagem, com o equipamento e o ambiente. É acessibilidade e é SEO de imagem.
- **`desafio` / `solucao` / `resultado`**: 1 a 2 frases cada, concretas, sem adjetivo de folheto. Fórmula: `desafio` = a condição do ambiente que gerou a chamada; `solucao` = o que foi instalado e onde; `resultado` = o efeito, com número só se for verificável.

`tipo` e `categoria` saem da tabela da spec (seção "Vocabulário de `categoria`"). Títulos e `h1` de cada case:

| slug | tipo | categoria | `seo.titulo` | `seo.h1` |
|---|---|---|---|---|
| `motor-classic` | evento | corporativo | Climatizador para exposição de carros · Case Motor Classic \| Nuvem Air | Motor Classic: público parado, carro quente, galpão fechado |
| `companhia-de-melhoramentos` | evento | corporativo | Climatização de evento corporativo · Case Companhia de Melhoramentos \| Nuvem Air | Um evento corporativo inteiro climatizado sem obra nenhuma |
| `rotary-sicredi` | evento | corporativo | Climatizador para jantar beneficente · Case Rotary Sicredi \| Nuvem Air | Jantar do Rotary com Sicredi: salão cheio, ninguém suando |
| `rotary-acema` | evento | corporativo | Aluguel de climatizador para evento de entidade · Case Rotary Acema \| Nuvem Air | Rotary Acema: climatização para um salão de associação |
| `hospital-universitario` | evento | institucional | Climatizador para hospital · Case Hospital Universitário \| Nuvem Air | Climatização num hospital, onde não cabe improviso |
| `weld-vision` | evento | industria | Climatizador industrial para solda · Case Weld Vision \| Nuvem Air | Weld Vision: calor de solda somado ao calor do galpão |
| `agro-semente` | evento | galpao | Climatizador para feira agro · Case Agro Semente \| Nuvem Air | Feira agro em galpão: climatizar o que não tem parede |
| `havam` | evento | galpao | Climatização de galpão · Case Havam \| Nuvem Air | Havam: um galpão que ficou utilizável no meio da tarde |
| `ecospeed` | evento | industria | Climatizador para empresa · Case Ecospeed \| Nuvem Air | Ecospeed: climatização pontual para a área de produção |
| `ciacollor` | evento | industria | Climatizador para indústria · Case Ciacollor \| Nuvem Air | Ciacollor: ar em movimento no chão de fábrica |
| `colegio-adventista` | evento | institucional | Climatizador para escola e formatura · Case Colégio Adventista \| Nuvem Air | Colégio Adventista: ginásio cheio em dia de evento |
| `quinta-dos-mouroes` | evento | casamento | Climatizador para espaço de eventos · Case Quinta dos Mourões \| Nuvem Air | Quinta dos Mourões: festa ao ar livre com ar fresco |
| `chacara-jandaia` | evento | casamento | Climatizador para chácara e festa · Case Chácara Jandaia \| Nuvem Air | Chácara em Jandaia: climatizar uma festa sem parede |
| `confraternizacao-empresa-jandaia` | evento | corporativo | Climatizador para confraternização de empresa · Case Jandaia \| Nuvem Air | Confraternização em Jandaia: o time inteiro, sem abafamento |
| `casamento-em-loanda` | evento | casamento | Aluguel de climatizador para casamento em Loanda · Nuvem Air | Um casamento em Loanda sem ninguém abanando o leque |
| `soft-dream-tecelagem` | mensal | industria | Climatizador para tecelagem · Locação mensal Soft Dream \| Nuvem Air | Soft Dream: climatização mensal numa tecelagem |

`seo.descricao`: 140–160 caracteres, começando pelo que foi feito e terminando em cidade + Nuvem Air. Dois exemplos prontos, para calibrar o resto:

- `casamento-em-loanda`: `Climatizadores evaporativos numa recepção de casamento em Loanda (PR): instalação discreta, sem ruído e sem obra. Veja o case da Nuvem Air.`
- `soft-dream-tecelagem`: `Locação mensal de climatizadores numa tecelagem: conforto térmico no chão de fábrica com baixo consumo. Veja o case da Nuvem Air em Maringá.`

Estrutura de cada entrada (exemplo completo, use como molde para os 16):

```json
{
  "publicar": false,
  "cases": [
    {
      "slug": "casamento-em-loanda",
      "tipo": "evento",
      "categoria": "casamento",
      "cliente": "Casamento em Loanda",
      "cidade": "Loanda",
      "uf": "PR",
      "data": "REVISAR: 2025-11",
      "equipamento": ["Climatizador Industrial Compacto"],
      "quantidade": "REVISAR: 4",
      "area": "REVISAR: 300 m²",
      "desafio": "Recepção ao ar livre sob tenda, em novembro, com os convidados concentrados nas mesas centrais.",
      "solucao": "Climatizadores evaporativos nas laterais da tenda, fora do campo de visão das fotos e longe da pista.",
      "resultado": "Ar em movimento durante toda a festa, sem ruído competindo com a música e sem cabo aparente.",
      "midia": [
        { "tipo": "imagem", "arquivo": "casamento-em-loanda-1.webp", "poster": null, "alt": "Climatizador evaporativo instalado na lateral da tenda de um casamento em Loanda" },
        { "tipo": "video", "arquivo": "casamento-em-loanda-2.mp4", "poster": "casamento-em-loanda-2.webp", "alt": "Climatizador em operação durante a recepção do casamento" }
      ],
      "seo": {
        "titulo": "Aluguel de climatizador para casamento em Loanda · Nuvem Air",
        "descricao": "Climatizadores evaporativos numa recepção de casamento em Loanda (PR): instalação discreta, sem ruído e sem obra. Veja o case da Nuvem Air.",
        "h1": "Um casamento em Loanda sem ninguém abanando o leque"
      }
    }
  ]
}
```

- [ ] **Step 6: Rodar os testes — agora tudo passa**

```bash
npm test
```

Esperado: PASS em todos, inclusive `o eventos.json real do projeto passa na validação, mídia em disco incluída`. Se falhar com "mídia ausente em disco", o nome no `eventos.json` divergiu do manifesto — corrija pelo manifesto, que é a verdade do que foi gerado.

- [ ] **Step 7: Commit**

```bash
git add eventos.json tools/eventos-dados.js tests/eventos.test.js
git commit -m "Adiciona os dados dos 16 cases de evento e sua validação

O módulo é importado pelo gerador de eventos e pelo gerar-loja.js, que é
quem escreve o sitemap — assim página nova e URL no sitemap não saem de
sincronia. A validação recusa slug com acento (404 na Vercel), categoria
fora do vocabulário e campo REVISAR pendente com publicar ligado."
```

---

### Task 3: Nav e rodapé como fonte única

As páginas geradas precisam do mesmo cabeçalho e rodapé do `index.html`. Duplicar o markup no gerador significaria que mudar o menu em um lugar deixaria o outro para trás. Este módulo extrai os dois blocos do próprio `index.html` e conserta os caminhos relativos.

**Files:**
- Create: `tools/site-partes.js`
- Create: `tests/site-partes.test.js`
- Modify: `index.html` (só inserir 4 comentários-marcadores)

**Interfaces:**
- Consumes: nada de tasks anteriores.
- Produces:
  - `extrair(html, marcador)` → `string` com o conteúdo entre `<!-- {marcador}:INICIO -->` e `<!-- {marcador}:FIM -->`. Lança se o marcador não existir.
  - `absolutizar(html)` → `string` com caminhos de asset e âncoras prefixados com `/`.
  - `partes(opcoes = {})` → `{ nav: string, rodape: string }`, já absolutizados. `opcoes.raiz` sobrescreve a raiz.

- [ ] **Step 1: Escrever os testes que falham**

Crie `tests/site-partes.test.js`:

```js
const test = require('node:test');
const assert = require('node:assert');
const partes = require('../tools/site-partes.js');

test('extrai o conteúdo entre os marcadores', () => {
  const html = 'antes\n<!-- NAV:INICIO -->\n<header>oi</header>\n<!-- NAV:FIM -->\ndepois';
  assert.strictEqual(partes.extrair(html, 'NAV').trim(), '<header>oi</header>');
});

test('lança quando o marcador não existe', () => {
  assert.throws(() => partes.extrair('<p>nada</p>', 'NAV'), /NAV/);
});

test('absolutiza src e href de assets relativos', () => {
  const r = partes.absolutizar('<img src="assets/img/logo.webp" /><a href="assets/x.pdf">x</a>');
  assert.ok(r.includes('src="/assets/img/logo.webp"'));
  assert.ok(r.includes('href="/assets/x.pdf"'));
});

test('absolutiza âncoras, que na raiz apontam para seções da home', () => {
  assert.ok(partes.absolutizar('<a href="#faq">FAQ</a>').includes('href="/#faq"'));
});

test('não mexe em caminho já absoluto nem em link externo', () => {
  const entrada = '<a href="/comprar">c</a><a href="https://wa.me/55">w</a><a href="mailto:a@b.c">m</a>';
  assert.strictEqual(partes.absolutizar(entrada), entrada);
});

test('partes() devolve o nav e o rodapé reais do index.html, já absolutos', () => {
  const p = partes.partes();
  assert.ok(p.nav.includes('class="nav"'), 'nav não veio');
  assert.ok(p.rodape.includes('class="footer"'), 'rodapé não veio');
  assert.ok(!/src="assets\//.test(p.nav), 'sobrou caminho relativo no nav');
  assert.ok(!/href="#/.test(p.rodape), 'sobrou âncora relativa no rodapé');
});
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
npm test
```

Esperado: FAIL — `Cannot find module '../tools/site-partes.js'`.

- [ ] **Step 3: Inserir os marcadores no `index.html`**

Quatro comentários HTML, nenhuma mudança de markup. O `<header class="nav" id="nav">` começa na linha 135 e seu `</header>` fecha na linha 281; o `<footer class="footer">` começa na 974 e o `whatsapp-float` termina logo antes do `<script src="assets/js/app.js...">`.

Antes de `<header class="nav" id="nav">`, na linha anterior:

```html
  <!-- NAV:INICIO -->
```

Depois do `</header>` que fecha o nav (o que vem logo antes de `<!-- HERO · editorial light -->`):

```html
  <!-- NAV:FIM -->
```

Antes de `<footer class="footer">`:

```html
  <!-- RODAPE:INICIO -->
```

Depois do `</a>` que fecha o `whatsapp-float` e antes de `<script src="assets/js/app.js`:

```html
  <!-- RODAPE:FIM -->
```

- [ ] **Step 4: Escrever `tools/site-partes.js`**

```js
/* Cabeçalho e rodapé do site, extraídos do index.html.
 *
 * Por que extrair em vez de duplicar: as páginas de evento precisam do mesmo
 * nav e do mesmo rodapé da home. Copiar o markup para dentro do gerador
 * garantiria que, no dia em que o menu mudar, metade do site fique para trás.
 *
 * O index.html usa caminhos relativos (assets/js/app.js). Uma página em
 * /eventos/<slug>/ está dois níveis abaixo — daí o absolutizar().
 */
'use strict';

const fs = require('node:fs');
const path = require('node:path');

function extrair(html, marcador) {
  const inicio = html.indexOf(`<!-- ${marcador}:INICIO -->`);
  const fim = html.indexOf(`<!-- ${marcador}:FIM -->`);
  if (inicio === -1 || fim === -1 || fim < inicio) {
    throw new Error(`marcador ${marcador} não encontrado no index.html — os comentários <!-- ${marcador}:INICIO --> e <!-- ${marcador}:FIM --> precisam existir`);
  }
  return html.slice(inicio + `<!-- ${marcador}:INICIO -->`.length, fim);
}

function absolutizar(html) {
  return html
    .replace(/(src|href)="assets\//g, '$1="/assets/')
    .replace(/href="#/g, 'href="/#');
}

function partes(opcoes = {}) {
  const raiz = opcoes.raiz || path.join(__dirname, '..');
  const html = fs.readFileSync(path.join(raiz, 'index.html'), 'utf8');
  return {
    nav: absolutizar(extrair(html, 'NAV')),
    rodape: absolutizar(extrair(html, 'RODAPE'))
  };
}

module.exports = { extrair, absolutizar, partes };
```

- [ ] **Step 5: Rodar os testes**

```bash
npm test
```

Esperado: PASS em todos, inclusive `partes() devolve o nav e o rodapé reais`.

- [ ] **Step 6: Conferir que o index.html não mudou de aparência**

```bash
git diff --stat index.html
```

Esperado: `index.html | 4 ++++` — só as quatro linhas de comentário. Se aparecer remoção, algo foi apagado por engano; reverta e refaça.

- [ ] **Step 7: Commit**

```bash
git add tools/site-partes.js tests/site-partes.test.js index.html
git commit -m "Extrai nav e rodapé do index.html para reúso nas páginas geradas

As páginas de evento precisam do mesmo cabeçalho da home. Duplicar o
markup no gerador faria o menu divergir na primeira alteração, então o
index.html ganha marcadores e vira a fonte. Caminhos relativos viram
absolutos porque /eventos/<slug>/ está dois níveis abaixo da raiz."
```

---

### Task 4: Gerador — páginas de case

**Files:**
- Create: `tools/gerar-eventos.js`
- Create: `assets/css/eventos.css`
- Create: `eventos/<slug>/index.html` (saída, 16 arquivos)

**Interfaces:**
- Consumes: `tools/eventos-dados.js` (`carregar`, `validar`, `irmaos`), `tools/site-partes.js` (`partes`).
- Produces: `tools/gerar-eventos.js` exporta `{ paginaCase, paginaListagem, gerar }` para a Task 5 completar e para inspeção; rodar o arquivo direto executa `gerar()`.

- [ ] **Step 1: Escrever `tools/gerar-eventos.js` com a página de case**

```js
#!/usr/bin/env node
/* Gerador estático dos cases de evento.  Roda dentro do `npm run build`.
 *
 * Por que sai em HTML pronto e não montado no navegador: é o mesmo motivo do
 * gerar-loja.js — página montada por JS chega ao Googlebot vazia, e o valor
 * inteiro desta seção é orgânico.
 */
'use strict';

const fs = require('node:fs');
const path = require('node:path');
const dados = require('./eventos-dados.js');
const sitePartes = require('./site-partes.js');

const RAIZ = path.join(__dirname, '..');
const SITE = process.env.SITE_URL || 'https://nuvemair.com.br';
const WA_NUM = '5544988117615';
const CSS_V = '20260911-1';

const esc = (s) => String(s == null ? '' : s)
  .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;');

const wa = (texto) => 'https://wa.me/' + WA_NUM + '?text=' + encodeURIComponent(texto);

/* Campo que ainda não foi revisado não vai para a página: melhor a frase
   existir sem o número do que sair "REVISAR: 300 m²" no ar. */
const limpo = (v) => (String(v == null ? '' : v).startsWith('REVISAR') ? '' : String(v == null ? '' : v));

const TIPO_ROTULO = { evento: 'Evento', mensal: 'Locação mensal' };

function head({ titulo, descricao, url, imagem, jsonld = [], noindex }) {
  return `<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <title>${esc(titulo)}</title>
  <meta name="description" content="${esc(descricao)}" />
  <meta name="author" content="Nuvem Air" />
  <meta name="robots" content="${noindex ? 'noindex, follow' : 'index, follow'}" />
  <link rel="canonical" href="${url}" />

  <meta property="og:type" content="article" />
  <meta property="og:site_name" content="Nuvem Air" />
  <meta property="og:locale" content="pt_BR" />
  <meta property="og:title" content="${esc(titulo)}" />
  <meta property="og:description" content="${esc(descricao)}" />
  <meta property="og:url" content="${url}" />
  <meta property="og:image" content="${imagem}" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content="${esc(titulo)}" />
  <meta name="twitter:description" content="${esc(descricao)}" />
  <meta name="twitter:image" content="${imagem}" />

  <meta name="theme-color" content="#1e2a78" />
  <link rel="icon" type="image/png" sizes="192x192" href="/assets/img/favicon-192.png?v=20260905-2" />
  <link rel="icon" type="image/png" sizes="96x96" href="/assets/img/favicon-96.png?v=20260905-2" />
  <link rel="icon" type="image/png" sizes="48x48" href="/assets/img/favicon-48.png?v=20260905-2" />
  <link rel="icon" type="image/png" sizes="192x192" media="(prefers-color-scheme: dark)" href="/assets/img/favicon-dark-192.png?v=20260905-2" />
  <link rel="apple-touch-icon" href="/assets/img/apple-touch-icon.png?v=20260905-2" />

  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=DM+Sans:opsz,wght@9..40,400;9..40,500;9..40,700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/assets/css/styles.css?v=${CSS_V}" />
  <link rel="stylesheet" href="/assets/css/eventos.css?v=${CSS_V}" />
${jsonld.map((o) => `  <script type="application/ld+json">\n${JSON.stringify(o, null, 2)}\n  </script>`).join('\n')}
</head>
<body>

`;
}

/* Um item de mídia. Vídeo reaproveita o lazy-load que o assets/js/app.js já
   faz (IntersectionObserver com rootMargin de 300px) — nenhum JS novo. */
function midia(c, m, primeiro) {
  const base = `/assets/eventos/${c.slug}/`;
  if (m.tipo === 'video') {
    return `        <figure class="case-midia__item">
          <video poster="${base}${esc(m.poster)}" class="lazy-video" data-src="${base}${esc(m.arquivo)}" muted loop playsinline preload="none" aria-label="${esc(m.alt)}"></video>
        </figure>`;
  }
  return `        <figure class="case-midia__item">
          <img src="${base}${esc(m.arquivo)}" alt="${esc(m.alt)}" width="1200" height="900" decoding="async" loading="${primeiro ? 'eager' : 'lazy'}" />
        </figure>`;
}

function ficha(c) {
  const linhas = [
    ['Tipo', TIPO_ROTULO[c.tipo]],
    ['Local', [limpo(c.cidade), limpo(c.uf)].filter(Boolean).join(' · ')],
    ['Quando', limpo(c.data)],
    ['Equipamento', (c.equipamento || []).join(', ')],
    ['Quantidade', limpo(c.quantidade)],
    ['Área', limpo(c.area)]
  ].filter(([, v]) => v);

  return `      <dl class="case-ficha">
${linhas.map(([r, v]) => `        <div class="case-ficha__linha"><dt>${esc(r)}</dt><dd>${esc(v)}</dd></div>`).join('\n')}
      </dl>`;
}

function paginaCase(conj, c, partes) {
  const url = `${SITE}/eventos/${c.slug}`;
  const capa = c.midia.find((m) => m.tipo === 'imagem') || c.midia[0];
  const imagem = `${SITE}/assets/eventos/${c.slug}/${capa.poster || capa.arquivo}`;
  const vizinhos = dados.irmaos(conj, c.slug, 2);
  const local = [limpo(c.cidade), limpo(c.uf)].filter(Boolean).join(' · ');

  /* Article, e não Event: o schema Event descreve evento futuro, com data e
     ingresso. Aplicá-lo a trabalho entregue gera rich result errado e é
     passível de ação manual no Search Console. */
  const jsonld = [
    {
      '@context': 'https://schema.org',
      '@type': 'Article',
      headline: c.seo.h1,
      description: c.seo.descricao,
      image: imagem,
      inLanguage: 'pt-BR',
      mainEntityOfPage: { '@type': 'WebPage', '@id': url },
      author: { '@type': 'Organization', name: 'Nuvem Air', url: SITE },
      publisher: { '@type': 'Organization', name: 'Nuvem Air', url: SITE }
    },
    {
      '@context': 'https://schema.org',
      '@type': 'BreadcrumbList',
      itemListElement: [
        { '@type': 'ListItem', position: 1, name: 'Início', item: SITE + '/' },
        { '@type': 'ListItem', position: 2, name: 'Eventos', item: SITE + '/eventos' },
        { '@type': 'ListItem', position: 3, name: c.cliente, item: url }
      ]
    }
  ];

  const blocos = [
    ['O desafio', limpo(c.desafio)],
    ['O que fizemos', limpo(c.solucao)],
    ['O resultado', limpo(c.resultado)]
  ].filter(([, v]) => v);

  return head({ titulo: c.seo.titulo, descricao: c.seo.descricao, url, imagem, jsonld, noindex: !conj.publicar })
    + partes.nav + `

  <main class="case">
    <div class="container">
      <nav class="case-trilha" aria-label="Trilha de navegação">
        <a href="/">Início</a> <span aria-hidden="true">/</span>
        <a href="/eventos">Eventos</a> <span aria-hidden="true">/</span>
        <span aria-current="page">${esc(c.cliente)}</span>
      </nav>

      <span class="case-selo case-selo--${esc(c.tipo)}">${esc(TIPO_ROTULO[c.tipo])}</span>
      <h1 class="case-titulo">${esc(c.seo.h1)}</h1>
      ${local ? `<p class="case-local">${esc(local)}</p>` : ''}

${ficha(c)}

      <div class="case-midia">
${c.midia.map((m, i) => midia(c, m, i === 0)).join('\n')}
      </div>

${blocos.map(([t, v]) => `      <section class="case-bloco">
        <h2>${esc(t)}</h2>
        <p>${esc(v)}</p>
      </section>`).join('\n')}

      <section class="case-cta">
        <h2>Precisa do mesmo para o seu espaço?</h2>
        <p>A equipe calcula o número de equipamentos pelas dimensões do lugar e devolve o orçamento em até 2 horas.</p>
        <a class="btn btn--primary" href="${wa(`Olá! Vi o case ${c.cliente} no site e quero um orçamento.`)}" target="_blank" rel="noopener">Pedir orçamento no WhatsApp</a>
      </section>

      <section class="case-irmaos">
        <h2>Outros trabalhos</h2>
        <div class="case-irmaos__grade">
${vizinhos.map((v) => {
  const vc = v.midia.find((m) => m.tipo === 'imagem') || v.midia[0];
  return `          <a class="case-irmaos__card" href="/eventos/${esc(v.slug)}">
            <img src="/assets/eventos/${esc(v.slug)}/${esc(vc.poster || vc.arquivo)}" alt="${esc(vc.alt)}" width="420" height="315" loading="lazy" decoding="async" />
            <strong>${esc(v.cliente)}</strong>
            <span>${esc(TIPO_ROTULO[v.tipo])}</span>
          </a>`;
}).join('\n')}
        </div>
        <a class="case-voltar" href="/eventos">&larr; Ver todos os trabalhos</a>
      </section>
    </div>
  </main>

` + partes.rodape + `
  <script src="/assets/js/app.js?v=20260905-5"></script>
</body>
</html>
`;
}

function gerar() {
  const conj = dados.validar(dados.carregar(), { raiz: RAIZ });
  const partes = sitePartes.partes();

  for (const c of conj.cases) {
    const pasta = path.join(RAIZ, 'eventos', c.slug);
    fs.mkdirSync(pasta, { recursive: true });
    fs.writeFileSync(path.join(pasta, 'index.html'), paginaCase(conj, c, partes));
    console.log('  -> eventos/%s/index.html', c.slug);
  }

  console.log('%d cases gerados%s', conj.cases.length, conj.publicar ? '' : ' (noindex — publicar:false)');
}

module.exports = { paginaCase, gerar };

if (require.main === module) gerar();
```

- [ ] **Step 2: Escrever `assets/css/eventos.css`**

Usa as variáveis que o `styles.css` já define (`--paper-2`, `--line`, `--ink-2`) e a classe `.container` que já existe, para a seção não destoar da home.

```css
/* Seção de eventos — cases.  Carregado depois do styles.css, que define as
   variáveis de cor e o .container. */

.case { padding: 140px 0 100px; }

.case-trilha { font-size: 13px; color: var(--ink-2); margin-bottom: 28px; }
.case-trilha a { color: var(--ink-2); text-decoration: none; }
.case-trilha a:hover { text-decoration: underline; }
.case-trilha span { margin: 0 6px; }

.case-selo {
  display: inline-block; font-size: 12px; letter-spacing: .08em; text-transform: uppercase;
  padding: 6px 12px; border: 1px solid var(--line); border-radius: 999px; color: var(--ink-2);
}
.case-selo--mensal { background: var(--paper-2); }

.case-titulo { font-size: clamp(30px, 5vw, 52px); line-height: 1.1; margin: 18px 0 10px; max-width: 16ch; }
.case-local { font-size: 16px; color: var(--ink-2); margin-bottom: 40px; }

.case-ficha {
  display: grid; grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
  gap: 1px; background: var(--line); border: 1px solid var(--line); margin-bottom: 48px;
}
.case-ficha__linha { background: var(--paper-2); padding: 18px 20px; }
.case-ficha dt { font-size: 12px; text-transform: uppercase; letter-spacing: .06em; color: var(--ink-2); margin-bottom: 6px; }
.case-ficha dd { font-size: 16px; margin: 0; }

.case-midia { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 14px; margin-bottom: 56px; }
.case-midia__item { margin: 0; aspect-ratio: 4 / 3; overflow: hidden; border-radius: 4px; background: var(--paper-2); }
.case-midia__item img,
.case-midia__item video { width: 100%; height: 100%; object-fit: cover; display: block; }

.case-bloco { max-width: 620px; margin-bottom: 36px; }
.case-bloco h2 { font-size: 22px; margin-bottom: 10px; }
.case-bloco p { font-size: 17px; color: var(--ink-2); }

.case-cta { border-top: 1px solid var(--line); margin-top: 56px; padding-top: 48px; max-width: 620px; }
.case-cta h2 { font-size: 26px; margin-bottom: 10px; }
.case-cta p { font-size: 17px; color: var(--ink-2); margin-bottom: 22px; }

.case-irmaos { border-top: 1px solid var(--line); margin-top: 64px; padding-top: 48px; }
.case-irmaos h2 { font-size: 20px; margin-bottom: 22px; }
.case-irmaos__grade { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 20px; margin-bottom: 28px; }
.case-irmaos__card { text-decoration: none; color: inherit; display: block; }
.case-irmaos__card img { width: 100%; aspect-ratio: 4 / 3; object-fit: cover; border-radius: 4px; margin-bottom: 10px; display: block; }
.case-irmaos__card strong { display: block; font-size: 16px; }
.case-irmaos__card span { font-size: 13px; color: var(--ink-2); }
.case-voltar { font-size: 15px; text-decoration: none; color: var(--ink-2); }
.case-voltar:hover { text-decoration: underline; }

@media (max-width: 640px) {
  .case { padding: 110px 0 72px; }
  .case-ficha { grid-template-columns: 1fr 1fr; }
}
```

- [ ] **Step 3: Gerar e conferir uma página**

```bash
node tools/gerar-eventos.js && ls eventos/
```

Esperado: 16 linhas `-> eventos/<slug>/index.html`, a linha de resumo com `(noindex — publicar:false)`, e 16 diretórios.

- [ ] **Step 4: Verificar o SEO da saída**

```bash
grep -c "REVISAR" eventos/*/index.html | grep -v ":0" || echo "OK: nenhum REVISAR vazou"
grep -h "<title>" eventos/*/index.html | sort | uniq -d || echo "OK: títulos únicos"
grep -o 'rel="canonical" href="[^"]*"' eventos/casamento-em-loanda/index.html
grep -c 'src="assets/' eventos/casamento-em-loanda/index.html || echo "OK: nenhum caminho relativo"
```

Esperado, nesta ordem: `OK: nenhum REVISAR vazou`; `OK: títulos únicos`; `rel="canonical" href="https://nuvemair.com.br/eventos/casamento-em-loanda"`; `OK: nenhum caminho relativo`.

- [ ] **Step 5: Validar o JSON-LD**

```bash
node -e "
const h=require('fs').readFileSync('eventos/casamento-em-loanda/index.html','utf8');
const m=[...h.matchAll(/<script type=\"application\/ld\+json\">([\s\S]*?)<\/script>/g)];
m.forEach(x=>{const o=JSON.parse(x[1]);console.log(o['@type'])});
"
```

Esperado: `Article` e `BreadcrumbList`, sem erro de parse. Se aparecer `Event`, está errado — veja a restrição global.

- [ ] **Step 6: Abrir no navegador e olhar**

```bash
python3 -m http.server 8765 >/dev/null 2>&1 &
open "http://localhost:8765/eventos/casamento-em-loanda/"
```

Confira: nav aparece e o logo carrega (caminho absoluto funcionando), vídeos tocam ao rolar, rodapé aparece, layout não quebra em 375 px de largura. Depois: `kill %1`.

- [ ] **Step 7: Commit**

```bash
git add tools/gerar-eventos.js assets/css/eventos.css eventos
git commit -m "Gera uma página por case de evento, com SEO próprio

Cada case sai com title, description, canonical, OG com o poster do
próprio trabalho e JSON-LD Article + BreadcrumbList. Article, e não
Event: Event descreve evento futuro e geraria rich result errado para
trabalho já entregue. Campos com REVISAR não vão para a página."
```

---

### Task 5: Gerador — listagem `/eventos`

**Files:**
- Modify: `tools/gerar-eventos.js` (adicionar `paginaListagem` e chamá-la em `gerar()`)
- Modify: `assets/css/eventos.css` (acrescentar o bloco da listagem)
- Create: `eventos/index.html` (saída)

**Interfaces:**
- Consumes: tudo da Task 4.
- Produces: `paginaListagem(conj, partes)` → `string`. Exportado junto com `paginaCase` e `gerar`.

- [ ] **Step 1: Acrescentar `paginaListagem` em `tools/gerar-eventos.js`**

Insira esta função logo antes de `function gerar()`:

```js
const CATEGORIA_ROTULO = {
  casamento: 'Casamentos',
  corporativo: 'Corporativos',
  industria: 'Indústria',
  galpao: 'Galpões',
  institucional: 'Institucional'
};

function paginaListagem(conj, partes) {
  const url = SITE + '/eventos';
  const titulo = 'Eventos e locações da Nuvem Air · 16 trabalhos entregues';
  const descricao = 'Casamentos, feiras, galpões, indústrias e hospitais climatizados pela Nuvem Air. Veja os equipamentos instalados em cada tipo de ambiente.';
  const capa = conj.cases[0];
  const capaMidia = capa.midia.find((m) => m.tipo === 'imagem') || capa.midia[0];

  const jsonld = [
    {
      '@context': 'https://schema.org',
      '@type': 'CollectionPage',
      name: titulo,
      description: descricao,
      url,
      inLanguage: 'pt-BR'
    },
    {
      '@context': 'https://schema.org',
      '@type': 'ItemList',
      itemListElement: conj.cases.map((c, i) => ({
        '@type': 'ListItem',
        position: i + 1,
        name: c.cliente,
        url: `${SITE}/eventos/${c.slug}`
      }))
    }
  ];

  /* Os 16 cards vêm no HTML e o filtro só esconde. Conteúdo que só existe
     depois do JS não é indexado — e o orgânico é o ponto desta seção. */
  const categorias = dados.CATEGORIAS.filter((k) => conj.cases.some((c) => c.categoria === k));

  return head({ titulo, descricao, url, imagem: `${SITE}/assets/eventos/${capa.slug}/${capaMidia.poster || capaMidia.arquivo}`, jsonld, noindex: !conj.publicar })
    + partes.nav + `

  <main class="lista">
    <div class="container">
      <nav class="case-trilha" aria-label="Trilha de navegação">
        <a href="/">Início</a> <span aria-hidden="true">/</span>
        <span aria-current="page">Eventos</span>
      </nav>

      <h1 class="lista__titulo">Onde a Nuvem Air já esteve.</h1>
      <p class="lista__sub">${esc(conj.cases.length)} trabalhos entregues — de casamento em chácara a chão de fábrica. Cada um com o equipamento que o ambiente pedia.</p>

      <div class="lista__filtros" role="group" aria-label="Filtrar trabalhos">
        <button class="lista__filtro is-ativo" data-filtro="todos">Todos</button>
        <button class="lista__filtro" data-filtro="mensal">Locação mensal</button>
${categorias.map((k) => `        <button class="lista__filtro" data-filtro="${esc(k)}">${esc(CATEGORIA_ROTULO[k])}</button>`).join('\n')}
      </div>

      <div class="lista__grade">
${conj.cases.map((c) => {
  const m = c.midia.find((x) => x.tipo === 'imagem') || c.midia[0];
  const local = [limpo(c.cidade), limpo(c.uf)].filter(Boolean).join(' · ');
  return `        <a class="lista__card" href="/eventos/${esc(c.slug)}" data-tipo="${esc(c.tipo)}" data-categoria="${esc(c.categoria)}">
          <img src="/assets/eventos/${esc(c.slug)}/${esc(m.poster || m.arquivo)}" alt="${esc(m.alt)}" width="420" height="315" loading="lazy" decoding="async" />
          <span class="lista__card-selo">${esc(TIPO_ROTULO[c.tipo])}</span>
          <strong>${esc(c.cliente)}</strong>
          ${local ? `<span class="lista__card-local">${esc(local)}</span>` : ''}
        </a>`;
}).join('\n')}
      </div>

      <p class="lista__vazio" hidden>Nenhum trabalho nesta categoria ainda.</p>

      <section class="case-cta">
        <h2>O próximo pode ser o seu.</h2>
        <p>Diga as dimensões do espaço e o tipo de evento. A equipe calcula quantos equipamentos são necessários e devolve o orçamento em até 2 horas.</p>
        <a class="btn btn--primary" href="${wa('Olá! Vi os cases no site e quero um orçamento.')}" target="_blank" rel="noopener">Pedir orçamento no WhatsApp</a>
      </section>
    </div>
  </main>

` + partes.rodape + `
  <script src="/assets/js/app.js?v=20260905-5"></script>
  <script>
    /* Filtro da listagem. Os cards já estão no HTML; isto só esconde.
       Aceita ?categoria=industria e ?tipo=mensal vindos do mega-menu da home. */
    (function () {
      var cards = [].slice.call(document.querySelectorAll('.lista__card'));
      var botoes = [].slice.call(document.querySelectorAll('.lista__filtro'));
      var vazio = document.querySelector('.lista__vazio');

      function aplicar(filtro) {
        var visiveis = 0;
        cards.forEach(function (c) {
          var casa = filtro === 'todos' || c.dataset.categoria === filtro || c.dataset.tipo === filtro;
          c.hidden = !casa;
          if (casa) visiveis++;
        });
        botoes.forEach(function (b) { b.classList.toggle('is-ativo', b.dataset.filtro === filtro); });
        if (vazio) vazio.hidden = visiveis > 0;
      }

      botoes.forEach(function (b) {
        b.addEventListener('click', function () {
          aplicar(b.dataset.filtro);
          history.replaceState(null, '', b.dataset.filtro === 'todos' ? '/eventos' : '/eventos?categoria=' + b.dataset.filtro);
        });
      });

      var q = new URLSearchParams(location.search);
      var inicial = q.get('categoria') || q.get('tipo');
      if (inicial && botoes.some(function (b) { return b.dataset.filtro === inicial; })) aplicar(inicial);
    })();
  </script>
</body>
</html>
`;
}
```

- [ ] **Step 2: Chamar a listagem em `gerar()` e exportá-la**

Em `gerar()`, depois do laço dos cases e antes do `console.log` final:

```js
  fs.mkdirSync(path.join(RAIZ, 'eventos'), { recursive: true });
  fs.writeFileSync(path.join(RAIZ, 'eventos', 'index.html'), paginaListagem(conj, partes));
  console.log('  -> eventos/index.html');
```

E troque a linha de export por:

```js
module.exports = { paginaCase, paginaListagem, gerar };
```

- [ ] **Step 3: Acrescentar o CSS da listagem em `assets/css/eventos.css`**

```css
/* Listagem /eventos */

.lista { padding: 140px 0 100px; }
.lista__titulo { font-size: clamp(32px, 5.5vw, 56px); line-height: 1.08; margin: 18px 0 12px; }
.lista__sub { font-size: 17px; color: var(--ink-2); max-width: 560px; margin-bottom: 36px; }

.lista__filtros { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 36px; }
.lista__filtro {
  font: inherit; font-size: 14px; cursor: pointer;
  padding: 9px 16px; border-radius: 999px;
  border: 1px solid var(--line); background: transparent; color: var(--ink-2);
}
.lista__filtro:hover { background: var(--paper-2); }
.lista__filtro.is-ativo { background: var(--ink); color: var(--paper); border-color: var(--ink); }

.lista__grade { display: grid; grid-template-columns: repeat(auto-fill, minmax(260px, 1fr)); gap: 24px; }
.lista__card { text-decoration: none; color: inherit; display: block; }
.lista__card[hidden] { display: none; }
.lista__card img { width: 100%; aspect-ratio: 4 / 3; object-fit: cover; border-radius: 4px; margin-bottom: 12px; display: block; }
.lista__card-selo { font-size: 11px; text-transform: uppercase; letter-spacing: .07em; color: var(--ink-2); display: block; margin-bottom: 4px; }
.lista__card strong { display: block; font-size: 17px; margin-bottom: 2px; }
.lista__card-local { font-size: 14px; color: var(--ink-2); }

.lista__vazio { font-size: 16px; color: var(--ink-2); padding: 40px 0; }

@media (max-width: 640px) {
  .lista { padding: 110px 0 72px; }
  .lista__grade { grid-template-columns: 1fr 1fr; gap: 16px; }
}
```

- [ ] **Step 4: Gerar e conferir**

```bash
node tools/gerar-eventos.js && ls eventos/index.html && grep -c 'class="lista__card"' eventos/index.html
```

Esperado: o arquivo existe e o `grep` devolve `16` — os 16 cards estão no HTML, não montados por JS.

- [ ] **Step 5: Conferir a listagem no navegador**

```bash
python3 -m http.server 8765 >/dev/null 2>&1 &
open "http://localhost:8765/eventos/"
open "http://localhost:8765/eventos/?categoria=industria"
```

Confira: os 16 cards aparecem; clicar num filtro reduz a grade e muda a URL; abrir com `?categoria=industria` já chega filtrado; clicar num card leva ao case certo. Depois: `kill %1`.

- [ ] **Step 6: Commit**

```bash
git add tools/gerar-eventos.js assets/css/eventos.css eventos/index.html
git commit -m "Adiciona a listagem /eventos com filtro por categoria

Os 16 cards vêm no HTML e o filtro só esconde — conteúdo montado por JS
não é indexado, e o orgânico é o ponto da seção. A querystring
?categoria= é a que o mega-menu da home vai usar."
```

---

### Task 6: A aba no nav da página de locação

**Files:**
- Modify: `index.html` (1 link novo no menu, 3 `href` de card do mega-menu)

**Interfaces:**
- Consumes: as rotas geradas nas Tasks 4 e 5.
- Produces: nada consumido por outra task.

- [ ] **Step 1: Acrescentar o link "Eventos" no menu**

Em `index.html`, logo depois da linha `<a class="nav__link nav__link--plain" href="#totem-led">Totem de LED</a>` (linha 155) e antes de `<div class="nav__item" data-menu="mercados">`:

```html
        <a class="nav__link nav__link--plain" href="/eventos">Eventos</a>
```

A classe `nav__link--plain` já existe e já é usada pelo FAQ — nenhum CSS novo.

- [ ] **Step 2: Apontar os cards do mega-menu "Mercados" para a listagem**

Os três cards do painel `data-panel="mercados"` apontam hoje todos para `#solucao`. Troque cada `href`:

| Card (pelo `nav__card-tag`) | `href` atual | `href` novo |
|---|---|---|
| `Indústria` (linha 242) | `#solucao` | `/eventos?categoria=industria` |
| `Eventos` (linha 247) | `#solucao` | `/eventos?categoria=casamento` |
| `Logística` (linha 252) | `#solucao` | `/eventos?categoria=galpao` |

Cada um é o `<a class="nav__card" href="#solucao">` imediatamente **antes** do `<span class="nav__card-tag">` indicado.

- [ ] **Step 3: Conferir o diff**

```bash
git diff index.html
```

Esperado: exatamente 4 linhas alteradas ou adicionadas — 1 link novo e 3 `href` trocados. Nada mais.

- [ ] **Step 4: Regerar (o nav é extraído do index.html) e verificar**

```bash
node tools/gerar-eventos.js && grep -c 'href="/eventos"' eventos/casamento-em-loanda/index.html
```

Esperado: ≥ 1 — a aba nova aparece também nas páginas geradas, porque o nav vem do `index.html`. Isto é a Task 3 funcionando.

- [ ] **Step 5: Conferir no navegador**

```bash
python3 -m http.server 8765 >/dev/null 2>&1 &
open "http://localhost:8765/"
```

Confira: "Eventos" aparece entre "Totem de LED" e "Mercados"; o pill do nav não quebrou nem estourou a largura; abrir o mega-menu "Mercados" e clicar em "Eventos & Casamentos" leva a `/eventos?categoria=casamento`. Confira também em 375 px. Depois: `kill %1`.

- [ ] **Step 6: Commit**

```bash
git add index.html eventos
git commit -m "Adiciona a aba Eventos ao nav da página de locação

Os três cards do mega-menu Mercados apontavam todos para a mesma âncora
#solucao; agora levam à listagem filtrada e o menu deixa de ser
decorativo."
```

---

### Task 7: Sitemap, rotas e integração ao build

**Files:**
- Modify: `tools/gerar-loja.js` (importar o módulo de dados e concatenar as URLs)
- Modify: `vercel.json` (2 redirects)
- Modify: `package.json` (o `build` passa a rodar os dois geradores)

**Interfaces:**
- Consumes: `tools/eventos-dados.js` (`carregar`, `urlsSitemap`).
- Produces: entrega final.

- [ ] **Step 1: Importar o módulo de eventos no `gerar-loja.js`**

Junto dos outros `require` do topo (perto de `const core = require('../loja/assets/js/loja-core.js');`):

```js
const eventosDados = require('./eventos-dados.js');
```

- [ ] **Step 2: Concatenar as URLs de evento no sitemap**

Em `tools/gerar-loja.js`, a constante `todasUrls` hoje é:

```js
const todasUrls = LOJA_ATIVA ? PAGINAS_FIXAS.concat(urls) : PAGINAS_FIXAS.slice();
```

Troque por:

```js
/* As URLs de evento vêm do módulo de dados, não de uma lista mantida aqui:
   página nova entrando no site sem entrar no sitemap é o erro clássico.
   Com publicar:false, urlsSitemap devolve vazio — mesma disciplina da loja
   fora do ar, que também fica fora daqui. */
const urlsEventos = eventosDados.urlsSitemap(eventosDados.carregar(), SITE);
const todasUrls = (LOJA_ATIVA ? PAGINAS_FIXAS.concat(urls) : PAGINAS_FIXAS.slice()).concat(urlsEventos);
```

- [ ] **Step 3: Ligar os dois geradores no `npm run build`**

Em `package.json`:

```json
  "scripts": {
    "build": "node tools/gerar-eventos.js && node tools/gerar-loja.js",
    "midia": "node tools/preparar-midia-eventos.js",
    "test": "node --test tests/*.test.js"
  }
```

- [ ] **Step 4: Acrescentar os redirects no `vercel.json`**

No array `redirects`, logo depois da entrada de `/comprar/index.html`:

```json
    {
      "source": "/eventos/index.html",
      "destination": "/eventos",
      "permanent": true
    },
    {
      "source": "/eventos/:slug/index.html",
      "destination": "/eventos/:slug",
      "permanent": true
    },
```

- [ ] **Step 5: Rodar o build completo**

```bash
npm run build
```

Esperado: as 16 linhas de case, a linha da listagem, depois a saída normal do gerador da loja, e no fim `-> sitemap.xml (2 URLs)` — **2**, porque `publicar` está `false` e os eventos ficam de fora de propósito.

- [ ] **Step 6: Confirmar que o sitemap respeita o `publicar`**

```bash
grep -c "eventos" sitemap.xml || echo "OK: eventos fora do sitemap (publicar:false)"
node -e "
const d=require('./tools/eventos-dados.js');
const c=d.carregar(); c.publicar=true;
console.log('com publicar:true ->', d.urlsSitemap(c,'https://nuvemair.com.br').length, 'URLs');
"
```

Esperado: `OK: eventos fora do sitemap (publicar:false)` e `com publicar:true -> 17 URLs`.

- [ ] **Step 7: Rodar a bateria de testes inteira**

```bash
npm test
```

Esperado: PASS em todos os arquivos de teste — `checkout`, `loja-carrinho`, `loja-core`, `eventos`, `site-partes`. Nenhum dos testes antigos pode ter quebrado.

- [ ] **Step 8: Conferir que nada da loja ou do /comprar mudou**

```bash
git status --short
```

Esperado: nenhuma modificação em `loja/`, `comprar/`, `api/`. Se `404.html` ou `robots.txt` aparecerem modificados, é saída normal do build — confira com `git diff` que a mudança é só de data.

- [ ] **Step 9: Commit**

```bash
git add tools/gerar-loja.js package.json vercel.json sitemap.xml robots.txt 404.html
git commit -m "Integra os eventos ao build, ao sitemap e às rotas

As URLs vêm do módulo de dados em vez de uma lista paralela no gerador do
sitemap. Com publicar:false elas ficam de fora de propósito: página
noindex dentro do sitemap é erro no Search Console."
```

---

### Task 8: Revisão do conteúdo com o usuário e publicação

Esta task não é código. É o portão que a spec definiu: nada com `REVISAR:` vai para o ar.

**Files:**
- Modify: `eventos.json` (correções do usuário + virar `publicar` para `true`)

**Interfaces:**
- Consumes: tudo.
- Produces: site publicado.

- [ ] **Step 1: Listar ao usuário tudo que precisa de revisão**

```bash
node -e "
const c=require('./eventos.json');
for (const x of c.cases) {
  const p = Object.entries(x).filter(([,v]) => String(v).includes('REVISAR'));
  if (p.length) console.log('\n' + x.slug + '\n  ' + p.map(([k,v]) => k + ': ' + v).join('\n  '));
}
"
```

Leve essa lista ao usuário. São os campos que não dá para verificar na mídia: cidade, data, quantidade, área.

- [ ] **Step 2: Aplicar as correções no `eventos.json`**

Substitua cada `REVISAR: <palpite>` pelo valor que o usuário informou. Campo que ele não souber: **apague o campo inteiro** em vez de deixar o palpite — a ficha técnica só imprime as linhas que têm valor, então some sem quebrar nada.

- [ ] **Step 3: Ligar a publicação**

Em `eventos.json`, primeira linha do objeto:

```json
  "publicar": true,
```

- [ ] **Step 4: Rodar build e testes com a publicação ligada**

```bash
npm run build && npm test
```

Esperado: `-> sitemap.xml (19 URLs)` (2 fixas + listagem + 16 cases) e todos os testes passando. Se o build falhar com `campo "x" ainda tem REVISAR`, é a validação fazendo o trabalho dela — volte ao Step 2.

- [ ] **Step 5: Confirmar que o noindex sumiu**

```bash
grep -h 'name="robots"' eventos/index.html eventos/casamento-em-loanda/index.html
```

Esperado: `content="index, follow"` nas duas linhas.

- [ ] **Step 6: Commit e publicação**

```bash
git add eventos.json eventos sitemap.xml
git commit -m "Publica a seção de eventos com o conteúdo revisado

Dados de cidade, data e dimensões confirmados pelo usuário; publicar
ligado, então as 17 URLs entram no sitemap e saem do noindex."
```

O deploy de produção segue o procedimento do projeto: sai da `main`, com `git fetch` antes, em worktree limpo — não a partir desta branch de trabalho.

---

## Auto-revisão do plano

**Cobertura da spec** — cada seção da spec tem task:

| Seção da spec | Task |
|---|---|
| Arquitetura / tabela de arquivos | 1–5, 7 |
| Por que `eventos-dados.js` existe | 2 (módulo), 7 (consumo no sitemap) |
| Fluxo de build | 7 |
| Formato do `eventos.json` + `publicar` | 2, 8 |
| Os 16 cases / slugs | 1 (mapa de origem), 2 (dados) |
| Vocabulário de `categoria` | 2 (validação), 5 (filtros), 6 (mega-menu) |
| SEO da página de case | 4 |
| SEO da listagem | 5 |
| Sitemap | 7 |
| A aba no nav | 6 |
| Roteamento na Vercel | 7 |
| Mídia: preset, fluxo, cache | 1 |
| Carregamento lazy | 4 (reúso de `app.js`) |
| Testes 1–6 | 2 |
| Fora de escopo | respeitado: nenhuma task toca `/loja`, `/comprar` ou `api/` |

**Consistência de tipos** — `carregar`, `validar`, `urlsSitemap`, `irmaos`, `CATEGORIAS`, `TIPOS` definidos na Task 2 e usados com a mesma assinatura nas Tasks 4, 5 e 7. `partes()` definido na Task 3, usado na 4 e na 5. `manifest.json` produzido na Task 1, consumido na 2. `paginaCase`/`paginaListagem`/`gerar` exportados na 4 e estendidos na 5.

**Desvio conhecido da spec:** a spec fala em "3–5 arquivos por case"; `colegio-adventista` e `ecospeed` têm 1 arquivo de origem cada, então ficam com 1. Está anotado na Task 1, Step 3.
